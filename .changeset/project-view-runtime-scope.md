---
'@xpert-ai/xpert-sdk': minor
---

Add Project and conversation runtime scope to View manifests, actions, uploads, file sessions, and host events so Agent and Project entry points can share one authoritative Project data scope.
