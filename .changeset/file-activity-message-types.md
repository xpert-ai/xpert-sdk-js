---
'@xpert-ai/xpert-sdk': patch
---

Add message file-change statistics and file delivery metadata to conversation APIs, including cancellation support for statistics requests. Define the required message and task-summary types within the SDK so consumers do not need `@xpert-ai/chatkit-types`.
